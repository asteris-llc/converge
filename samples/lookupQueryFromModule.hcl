/*This module demonstrates how lookups are handled relative to the
current module by demonstrating that including a modules does not
change the path used for calls to `lookup` */
module "query.hcl" "query-test" {}
